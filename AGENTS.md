# 상담왕 등극기 🏆 — 프로젝트 문서

> 협업자를 위한 프로젝트 현황, 구조, 작업 이력 문서입니다.  
> **매 작업 완료 후 반드시 업데이트해주세요.**

---

## 📌 프로젝트 개요

| 항목 | 내용 |
|---|---|
| 서비스명 | 상담왕 등극기 |
| 대상 | 엘리하이 상담 매니저 |
| 목적 | 상담 퀴즈를 통한 제품 지식 향상 + 포인트 적립 |
| 배포 URL | https://cje1520-collab.github.io/-/ |
| 어드민 URL | https://cje1520-collab.github.io/-/admin.html |
| 저장소 | https://github.com/cje1520-collab/- |

---

## 🗂️ 파일 구조

```
상담퀴즈너굴게임/
├── files/                   ← 실제 서비스 파일 (여기서 작업)
│   ├── index.html           ← 게임 메인
│   ├── admin.html           ← 어드민 패널
│   └── sw.js                ← 서비스 워커 (캐시)
├── index.html               ← GitHub 배포용 (files/에서 복사)
├── admin.html               ← GitHub 배포용 (files/에서 복사)
├── sw.js                    ← GitHub 배포용
├── backup_YYYYMMDD/         ← 작업 전 백업 폴더
│   ├── index.html.bak
│   └── admin.html.bak
├── mockup_roulette.html     ← 디자인 목업 (참고용)
└── AGENTS.md                ← 이 문서
```

> ⚠️ `files/`에서 수정 후 → `index.html`, `admin.html`에 복사 → GitHub 푸시

---

## 🛠️ 기술 스택

| 항목 | 내용 |
|---|---|
| 프론트엔드 | Vanilla HTML/CSS/JS (빌드 없음) |
| 데이터베이스 | Supabase (PostgreSQL) |
| 배포 | GitHub Pages |
| 폰트 | Google Fonts (Jua, Nanum Gothic) |

### Supabase 테이블 구조

**`quiz_results`** — 퀴즈 정답 기록
```
id, name, phone, question_id, points, created_at
```

**`questions`** — 퀴즈 문제
```
id, q, answer, hint, cat, fb_ok, fb_ng, active, sort_order
```

**`settings`** — 게임 설정값 (key-value)
```
key, value (JSON), updated_at

주요 키:
- game_texts      : 인트로/대시보드/직급 텍스트
- game_config     : 일일 제한, 상품권 기준 pts
- notice          : 공지사항
- roulette_config : 롤플레잉 룰렛 카테고리/항목
```

---

## 📱 화면 구조

```
인트로 (s-intro)
  └→ 로그인 (s-login)
       └→ 대시보드 (s-dashboard)
            ├→ [상담 퀴즈] → 퀴즈 (s-quiz) → 결과 (s-result) → 오답 복습 (s-review)
            ├→ [롤플레잉 룰렛] → 룰렛 (s-roulette)
            ├→ [랭킹] → 랭킹 모달 (rank-modal)
            └→ [내 성장 보기] → 성장 (s-growth)
```

---

## 🎮 주요 기능

### 상담 퀴즈
- 50문항 텍스트 입력 퀴즈
- 정답 시 포인트 적립 (1회차 20pts / 2~3회차 10pts / 4회차+ 5pts)
- 오답 시 wrongAnswers 배열에 저장 → 결과 화면에서 복습 가능
- 일일 문제 수 제한 (어드민 설정 가능)
- 스트릭(연속 정답) 시스템

### 직급 시스템
| 직급 | 포인트 범위 |
|---|---|
| 😊 신입 상담사 | 0 ~ 999 pts |
| 📚 열공 상담사 | 1,000 ~ 2,999 pts |
| ☕ 여유 상담사 | 3,000 ~ 5,999 pts |
| 🏅 베테랑 상담사 | 6,000 ~ 9,999 pts |
| 👑 전설의 상담왕 | 10,000 pts+ |

### 롤플레잉 룰렛
- 드럼 슬롯 방식으로 주제 랜덤 선택
- STOP 버튼으로 멈춤
- 카테고리/항목 어드민에서 자유 편집 가능

### 랭킹
- **이달의 TOP 5** (월간, 퀴즈 점수 기준)
- 나의 순위 표시

---

## ⚙️ 어드민 패널 사용법

**접속:** `/admin.html` → 비밀번호 입력

| 탭 | 기능 |
|---|---|
| 📈 현황 | 직급 분포, 전체 사용자 목록, 상품권 대상 관리 |
| 📝 문제 관리 | 퀴즈 문제 추가/수정/삭제/활성화 |
| 🏆 랭킹 | 전체 누적 랭킹 조회, 랭킹 초기화 |
| 📅 이번 달 | 이번 달 활동 TOP 현황 |
| ✏️ 텍스트 | 인트로/직급명/응원메시지/호칭 수정 |
| ⚙️ 설정 | 일일 제한 문항 수, 상품권 기준 pts |
| 📢 공지 | 대시보드 공지사항 관리 |
| 🎰 룰렛 | 롤플레잉 룰렛 카테고리/항목 편집 |

---

## 📋 작업 규칙

1. **작업 전** 반드시 내용 보고 → 담당자 컨펌 후 진행
2. **작업 전** `backup_YYYYMMDD/` 폴더에 백업 생성
3. **작업 후** `files/`에서 루트로 복사 → GitHub 푸시
4. **작업 후** 이 문서 하단 작업 이력 업데이트

---

## 📅 작업 이력

### 2026-08-07 (홈 UI 프리미엄 리디자인)
**기존 기능을 유지한 채 모바일 홈 화면을 현대적인 교육 앱 스타일로 개선**
- 점수·직급·성장률 영역을 한 장의 입체형 성장 대시보드로 재구성
- 상담 퀴즈·상담 핵심카드·교육자료에 부드러운 깊이감, 큰 글자, 명확한 색상 위계 적용
- 룰렛·랭킹·성장가이드는 작은 보조 메뉴로 유지하면서 터치 영역을 충분히 확보
- 상담마스터 전용 3D 너구리 학습 코치 캐릭터 자산 추가
- 기존 퀴즈·포인트·룰렛·랭킹·성장 기능 및 클릭 연결은 변경하지 않음
- 서비스 워커 캐시 버전을 `2026-08-07-v17`로 갱신

### 2026-08-07 (거절극복 추천 콘텐츠 구분)
**거절극복 콘텐츠도 상담카드와 같은 줄바꿈 관리 방식으로 통일**
- 거절극복 3단계 명칭을 `추천 콘텐츠`로 변경
- 줄바꿈·가운데점·쉼표로 구분된 콘텐츠를 화면에서 각각의 태그로 표시
- 어드민의 거절극복 추천 콘텐츠 입력란을 여러 줄 입력창으로 변경
- 저장 시 줄바꿈별 콘텐츠를 배열로 보관하고 기존 단일 문자열 데이터도 호환
- 서비스 워커 캐시 버전을 `2026-08-07-v16`으로 갱신

### 2026-08-07 (아이폰 PDF 폭 맞춤 뷰어)
**아이폰 Safari에서 PDF가 확대되어 좌우가 잘리는 문제 해결**
- 브라우저 기본 PDF iframe 대신 Mozilla PDF.js 기반 자체 뷰어 적용
- 문서 페이지를 기기 화면 폭에 자동 맞춤 렌더링
- 이전·다음 페이지 이동과 축소·확대 버튼 추가
- 상단 `교육자료` 복귀와 `원본` 열기 기능 유지
- 서비스 워커 캐시 버전을 `2026-08-07-v15`로 갱신

### 2026-08-07 (아이폰 가독성 보강)
**50대 상담 매니저 사용을 고려한 여백·글자 크기 개선**
- 홈 신규 메뉴에 좌우 여백을 추가하고 핵심·보조 메뉴 글자를 확대
- 상담카드·교육자료 화면을 아이폰 가장자리에서 8~12px 띄워 답답한 전체 폭 표시 개선
- 상담카드 질문·공감·추천 문구, 선택 버튼, 태그 글자를 약 15~25% 확대
- 교육자료 제목·연월·PDF 정보·열기 버튼 글자를 확대
- PDF·영상 전용 화면의 복귀 버튼과 제목 글자를 확대
- 서비스 워커 캐시 버전을 `2026-08-07-v14`로 갱신

### 2026-08-07 (PDF 복귀 화면 추가)
**상담력 인사이트 열람 후 교육자료로 돌아가는 전용 PDF 화면 구현**
- `pdf-viewer.html`에 상단 고정 `← 교육자료` 버튼과 PDF 내장 화면 추가
- 교육자료의 PDF 링크를 전용 화면으로 연결해 브라우저 빈 탭과 복귀 불편 개선
- PDF 원본 열기 보조 링크와 잘못된 파일 경로 차단 처리 추가
- 서비스 워커 캐시 버전을 `2026-08-07-v12`로 갱신

### 2026-08-07 (교육 영상 복귀 화면 추가)
**교육 영상도 상담력 인사이트와 동일한 복귀 흐름으로 통일**
- `video-viewer.html`에 상단 고정 `← 교육자료` 버튼과 YouTube 내장 재생 화면 추가
- 연계 영상 링크를 새 탭 대신 전용 영상 화면으로 연결
- YouTube 원본 열기 보조 링크와 영상 ID 검증 처리 추가
- 서비스 워커 캐시 버전을 `2026-08-07-v13`으로 갱신

### 2026-08-06 (상담카드·교육자료 로컬 통합)
**기존 운영 기능을 유지한 채 신규 콘텐츠 메뉴와 관리자 편집 기능 추가**
- 홈을 `자주 쓰는 메뉴`(상담퀴즈·상담 핵심카드·교육자료)와 `더보기`(룰렛·랭킹·성장가이드)로 재구성
- 기존 상담퀴즈·롤플레잉 룰렛·랭킹·성장·포인트 함수 연결은 그대로 유지
- 상담 핵심카드 29개와 거절극복 9개를 학년·과목별 모바일 화면으로 추가
- 2025년 7월~2026년 8월 상담력 인사이트 PDF 14개와 연계 영상 링크 통합
- 어드민에 `상담카드`, `교육자료` 탭 추가: 개별 항목·제목·PDF 주소·영상 링크·노출 여부 편집 지원
- 신규 설정 키 `counseling_cards_v1`, `education_archive_v1`만 사용해 기존 설정 및 점수 데이터와 분리
- PDF를 서비스 워커 캐시에 저장하지 않도록 처리하고 캐시 버전을 `2026-08-06-v10`으로 갱신
- 로컬 미리보기에서 홈, 상담카드, 거절극복, 교육자료, 어드민 편집 화면 클릭 동작 검수
- `files/`와 루트 배포용 파일을 동기화했으며 GitHub 배포는 아직 진행하지 않음

### 2026-08-06 (신규 화면 표시 오류 수정)
**실사용 미리보기에서 확인된 정렬·폰트·PDF 이동 문제 보완**
- 상담카드·교육자료 화면을 기존 모바일 `.wrap` 내부로 이동해 데스크톱에서도 가운데 정렬되도록 수정
- 홈 핵심 메뉴와 신규 콘텐츠 화면을 모바일 시스템 고딕 글꼴로 통일
- PDF 링크를 새 빈 탭이 아닌 현재 화면에서 직접 열도록 변경해 `브라우징 시작` 빈 화면 방지
- 로컬 HTTP 환경에서 2026년 8월 PDF 실제 주소 이동 확인
- 서비스 워커 캐시 버전을 `2026-08-06-v11`로 갱신

### 2026-08-06 (상담카드 시뮬레이터 재구성)
**엑셀 원본 기반 상담카드·거절극복 미리보기 구현**
- `상담카드_운영관리_분류수정_2026-07-29.xlsx`의 상담카드 30개를 학년·관심사별로 탐색 가능하게 반영
- 상담 핵심 흐름을 질문 어프로치 → 공감대 형성 → 추천 콘텐츠 → 추천 멘트로 구성
- 누락됐던 `거절반론` 9개 유형을 별도 `거절극복` 탭으로 추가
- 거절극복 카드를 먼저 공감 → 확인 질문 → 연결 콘텐츠 → 응대 멘트 → 마무리 연결 순서로 구성
- 상담 퀴즈·롤플레잉 룰렛·상담 핵심카드·교육자료가 한눈에 보이는 통합 홈과 각 화면의 홈 이동 버튼 복원
- 사용자 수정 엑셀의 추천 콘텐츠를 기준으로 완성한 상담 핵심카드 29개를 시뮬레이터에 교체 반영 (기존 퀴즈·룰렛·교육자료 비변경)
- 운영 서버에는 반영하지 않고 로컬 시뮬레이터에서만 검증

### 2026-07-28 (객관식 전환 준비)
**운영 중 엑셀 기반 객관식 문항 전환 및 포인트 정합성 개선**
- 기존 주관식 문항을 유지하면서 문항별 객관식 보기 4개와 공개 상태를 준비할 수 있도록 확장
- 어드민에 `.xlsx` 관리 양식 다운로드, 업로드 검증, 변경 미리보기 및 일괄 반영 추가
- 문제 ID 기준 업데이트로 엑셀 행 삭제가 실제 문항 삭제로 이어지지 않도록 보호
- 어드민의 미사용 문제별 200점 입력을 제거하고 실제 회차별 지급 포인트를 게임 설정으로 통합
- 첫 정답/2~3회차/4회차 이상 포인트 설정을 실제 게임 계산에 연동
- 기존에 저장만 되던 1회 출제 문제 수와 상품권 기준 설정을 실제 게임·어드민 집계에 연동
- Supabase 객관식 필드 추가 스크립트 `supabase_multiple_choice.sql` 작성
- 서비스 워커 캐시 버전을 `2026-07-28-v7`로 갱신

### 2026-07-28
**출제 균형·랭킹·접속 및 참여자 관리 개선**
- 문제 카테고리를 순환 배치해 특정 유형이 초반 문제에 편중되는 현상 개선
- 월간 랭킹 점수와 전체 누적 포인트를 분리하고, 월간 화면의 직급은 누적 포인트 기준으로 표시
- 로그인 시 일별 접속 이벤트를 저장하는 `access_logs` 연동 추가
- 어드민에 참여자 조회 탭 추가 (누적/월간 포인트, 최초/최근 접속일, 접속 일수)
- 참여자 목록 및 일별 접속 기록 CSV 다운로드 추가
- 기존 데이터 비변경 방식의 Supabase 생성 스크립트 `supabase_access_logs.sql` 추가
- 서비스 워커 캐시 버전을 `2026-07-28-v6`으로 갱신

### 2026-06-04
**초기 세팅 & 주요 기능 추가**
- `rand()` 함수 누락으로 인한 다음 문제 버튼 미노출 버그 수정
- GitHub Pages 배포 연결 (cje1520-collab/-)
- 대시보드 2×2 메뉴 그리드 추가 (퀴즈/룰렛/랭킹/성장)
- 롤플레잉 룰렛 기능 추가 (드럼 슬롯 + STOP)
- 랭킹 모달 추가 (나의 순위 + 월간 TOP5)
- 어드민 🎰 룰렛 탭 추가 (카테고리/항목 추가·수정·삭제)
- "대시보드" → "홈으로" 텍스트 전체 교체
- 매니저 호칭 기본값 변경 (선생님 → 매니저)

**버그 수정**
- 나가기 버튼 버그: `answered=true` 직후 nav 버튼 즉시 비활성화
- 랭킹 모달 `r.total` → `r.pts` 참조 오류 수정
- 다음 문제 버튼: 플로팅 고정 제거 → 문제 아래 중앙 인라인 표시
- `navAction()` 분리: 나가기는 항상 나가기, 다음 문제는 btn-next로만

**어드민 기능 강화**
- 대시보드 메뉴 카드 4개(퀴즈/룰렛/랭킹/성장) 제목·설명 어드민에서 편집 가능
- 어드민 ✏️ 텍스트 탭 → 🗂️ 대시보드 메뉴 카드 섹션 추가
- GT 기본값에 `menu_*_label/desc` 추가, `applyTexts()` 연동

**협업 문서**
- `AGENTS.md` 최초 생성 (프로젝트 구조, 기술 스택, 어드민 사용법, 작업 이력)
- 작업 전 보고 → 컨펌 → 진행 워크플로우 확립
- 작업 전 `backup_YYYYMMDD/` 백업 생성 규칙 확립

---

## 🔜 향후 예정 작업 (미확정)

- [ ] 롤플레잉 룰렛 주제별 스크립트 힌트 표시
- [ ] 추가 카테고리 (가격안내, 체험상담 등)
- [ ] 오답노트 강화 (날짜별 분류)


---

### 2026-08-06 (상담력 인사이트 아카이브 시뮬레이션)
**월별 PDF 교육자료 목록 및 제목 분리 관리 구조 추가**
- 2025년 7월부터 2026년 8월까지 상담력 인사이트 PDF 14개를 시뮬레이터 교육자료 화면에 최신순으로 구성
- 모바일 목록에 연월, 주제 제목, PDF 페이지 수를 표시하고 각 원본 PDF 열기 연결
- PDF 파일 경로와 화면 표시 제목을 `insight-archive.json` 데이터로 분리해 향후 어드민 제목 수정 구조 준비
- 기존 상담 퀴즈, 롤플레잉 룰렛, 상담 핵심카드 화면은 변경하지 않음
- 390px 모바일 화면 및 최신 PDF 연결 동작 확인


---

### 2026-08-06 (상담력 인사이트-교육영상 연계 시뮬레이션)
**월별 자료 카드 안에 PDF와 연계 영상 통합**
- 2026년 8월 인사이트에 신규 YouTube 교육 영상 연결
- 기존 교육 영상은 2026년 3월 인사이트에 연결하고 별도 교육 영상 카드는 제거
- 영상 보유 월에는 같은 카드에서 `PDF 읽기`와 `영상 보기`를 선택하도록 모바일 UI 구성
- 운영 파일과 비교해 시뮬레이터의 퀴즈·룰렛 내부 화면은 간이 목업이며 실제 운영 화면은 변경되지 않았음을 확인
- 390px 모바일 화면에서 14개 자료, 영상 2개, 가로 넘침 없음 확인


---

### 2026-08-06 (상담카드 모바일 디자인 개선 시뮬레이션)
**한글 가독성 및 연령별 카드 구분 강화**
- 전체 시뮬레이터 글꼴을 모바일 한글 가독성이 높은 시스템 고딕 계열로 변경
- 상담카드를 유아·초등 저학년·초등 중학년·초등 고학년·중등의 5개 색상 테마로 구분
- 연령별 병아리·토끼·여우·너구리·호랑이 캐릭터 표식 추가
- 선택 학년 버튼, 카드 테두리, 단계 번호, 콘텐츠 태그에 동일한 연령 색상 적용
- 390px 모바일 화면에서 유아 및 중등 카드 가독성, 버튼 간격, 가로 넘침 없음 확인


---

### 2026-08-06 (실서버 반영 전 안전 준비)
**운영본 백업·시뮬 보관·반영 위험 검토**
- 운영 루트와 `files`의 index/admin/service worker/.nojekyll을 `backup_20260806_before_cards_education`에 보관
- 백업 8개 운영 파일의 SHA-256이 원본과 모두 일치함을 확인하고 복구 안내서 작성
- 상담카드·교육자료 최종 시뮬, 데이터, 원본 엑셀, PDF 14개를 `simulation_20260806_cards_education`에 별도 보관
- 기존 퀴즈·포인트·랭킹·성장·룰렛 비변경 원칙과 회귀 테스트 목록을 `IMPLEMENTATION_REVIEW_20260806.md`에 작성
- PDF 약 24MB의 지연 로딩·캐시 제외 필요성과 향후 어드민 PDF 업로드 방식 결정을 확인
- 운영 파일 수정과 GitHub 배포는 아직 진행하지 않음


---

### 2026-08-06 (모바일 홈 메뉴 위계 시뮬레이션)
**핵심 3개·보조 3개 기능으로 홈 UX 재구성**
- 상담퀴즈를 전체 폭 핵심 카드로, 상담카드와 교육자료를 큰 2열 카드로 구성
- 룰렛·랭킹·성장가이드는 하단의 작은 빠른 메뉴 3개로 구성
- 320px에서는 핵심 카드 세로 배치, 360px 이상에서는 1+2열 배치하도록 반응형 적용
- 320·360·390·430px 화면에서 가로 넘침, 버튼 겹침, 주요 텍스트 영역 확인
- 최종 시뮬 버전을 14로 갱신하고 프로젝트 내 시뮬 보관본과 반영 검토 문서 동기화
- 운영 파일 수정과 GitHub 배포는 아직 진행하지 않음
