# 상담왕 등극기 🏆 — 프로젝트 문서

> 협업자를 위한 프로젝트 현황, 구조, 작업 이력 문서입니다.  
> **매 작업 완료 후 반드시 업데이트해주세요.**

---

## 📌 프로젝트 개요

| 항목 | 내용 |
|---|---|
| 서비스명 | 상담왕 등극기 |
| 대상 | 엘리하이 상담 매니저 |
| 목적 | 상담 퀴즈를 통한 제품 지식 향상 + 포인트 적립 |
| 배포 URL | https://cje1520-collab.github.io/-/ |
| 어드민 URL | https://cje1520-collab.github.io/-/admin.html |
| 저장소 | https://github.com/cje1520-collab/- |

---

## 🗂️ 파일 구조

```
상담퀴즈너굴게임/
├── files/                   ← 실제 서비스 파일 (여기서 작업)
│   ├── index.html           ← 게임 메인
│   ├── admin.html           ← 어드민 패널
│   └── sw.js                ← 서비스 워커 (캐시)
├── index.html               ← GitHub 배포용 (files/에서 복사)
├── admin.html               ← GitHub 배포용 (files/에서 복사)
├── sw.js                    ← GitHub 배포용
├── backup_YYYYMMDD/         ← 작업 전 백업 폴더
│   ├── index.html.bak
│   └── admin.html.bak
├── mockup_roulette.html     ← 디자인 목업 (참고용)
└── AGENTS.md                ← 이 문서
```

> ⚠️ `files/`에서 수정 후 → `index.html`, `admin.html`에 복사 → GitHub 푸시

---

## 🛠️ 기술 스택

| 항목 | 내용 |
|---|---|
| 프론트엔드 | Vanilla HTML/CSS/JS (빌드 없음) |
| 데이터베이스 | Supabase (PostgreSQL) |
| 배포 | GitHub Pages |
| 폰트 | Google Fonts (Jua, Nanum Gothic) |

### Supabase 테이블 구조

**`quiz_results`** — 퀴즈 정답 기록
```
id, name, phone, question_id, points, created_at
```

**`questions`** — 퀴즈 문제
```
id, q, answer, hint, cat, fb_ok, fb_ng, active, sort_order
```

**`settings`** — 게임 설정값 (key-value)
```
key, value (JSON), updated_at

주요 키:
- game_texts      : 인트로/대시보드/직급 텍스트
- game_config     : 일일 제한, 상품권 기준 pts
- notice          : 공지사항
- roulette_config : 롤플레잉 룰렛 카테고리/항목
```

---

## 📱 화면 구조

```
인트로 (s-intro)
  └→ 로그인 (s-login)
       └→ 대시보드 (s-dashboard)
            ├→ [상담 퀴즈] → 퀴즈 (s-quiz) → 결과 (s-result) → 오답 복습 (s-review)
            ├→ [롤플레잉 룰렛] → 룰렛 (s-roulette)
            ├→ [랭킹] → 랭킹 모달 (rank-modal)
            └→ [내 성장 보기] → 성장 (s-growth)
```

---

## 🎮 주요 기능

### 상담 퀴즈
- 50문항 텍스트 입력 퀴즈
- 정답 시 포인트 적립 (1회차 20pts / 2~3회차 10pts / 4회차+ 5pts)
- 오답 시 wrongAnswers 배열에 저장 → 결과 화면에서 복습 가능
- 일일 문제 수 제한 (어드민 설정 가능)
- 스트릭(연속 정답) 시스템

### 직급 시스템
| 직급 | 포인트 범위 |
|---|---|
| 😊 신입 상담사 | 0 ~ 999 pts |
| 📚 열공 상담사 | 1,000 ~ 2,999 pts |
| ☕ 여유 상담사 | 3,000 ~ 5,999 pts |
| 🏅 베테랑 상담사 | 6,000 ~ 9,999 pts |
| 👑 전설의 상담왕 | 10,000 pts+ |

### 롤플레잉 룰렛
- 드럼 슬롯 방식으로 주제 랜덤 선택
- STOP 버튼으로 멈춤
- 카테고리/항목 어드민에서 자유 편집 가능

### 랭킹
- **이달의 TOP 5** (월간, 퀴즈 점수 기준)
- 나의 순위 표시

---

## ⚙️ 어드민 패널 사용법

**접속:** `/admin.html` → 비밀번호 입력

| 탭 | 기능 |
|---|---|
| 📈 현황 | 직급 분포, 전체 사용자 목록, 상품권 대상 관리 |
| 📝 문제 관리 | 퀴즈 문제 추가/수정/삭제/활성화 |
| 🏆 랭킹 | 전체 누적 랭킹 조회, 랭킹 초기화 |
| 📅 이번 달 | 이번 달 활동 TOP 현황 |
| ✏️ 텍스트 | 인트로/직급명/응원메시지/호칭 수정 |
| ⚙️ 설정 | 일일 제한 문항 수, 상품권 기준 pts |
| 📢 공지 | 대시보드 공지사항 관리 |
| 🎰 룰렛 | 롤플레잉 룰렛 카테고리/항목 편집 |

---

## 📋 작업 규칙

1. **작업 전** 반드시 내용 보고 → 담당자 컨펌 후 진행
2. **작업 전** `backup_YYYYMMDD/` 폴더에 백업 생성
3. **작업 후** `files/`에서 루트로 복사 → GitHub 푸시
4. **작업 후** 이 문서 하단 작업 이력 업데이트

---

## 📅 작업 이력

### 2026-08-28 (기능 첫 안내·통합 도움말 시뮬레이션)
**상담 MASTER의 기능을 처음 접하는 사용자를 위한 클릭형 안내 흐름 제안**
- 홈 기능을 처음 누르면 용도와 위치를 짧게 설명하는 하단 알럿 구성
- 알럿에서 `다시 보지 않기`와 `바로 시작`을 선택하는 흐름 구현
- 오른쪽 상단 `도움말`에서 전체 기능과 메뉴 위치를 언제든 다시 확인하도록 구성
- 도움말에서 기능 첫 안내를 켜고 끄는 설정과 기능별 상세 안내 연결
- 피드백 반영본에서는 메뉴 위치·경로 문구를 제거하고 기능 설명과 `바로 시작`만 유지
- 첫 접속 시 통합 안내를 자동 노출하고 `7일 동안 보지 않기` 선택 흐름 추가
- 오른쪽 상단 도움말의 화살표를 누르면 기능별 설명과 바로 시작 버튼이 열리도록 간소화
- 첫 자동 노출을 특정 상담퀴즈 안내가 아닌 전체 `상담 MASTER 사용 가이드` 목록으로 변경
- 운영 index·admin·service worker 및 Supabase 데이터는 변경하지 않음
- 백업: `backup_20260828_before_feature_guide_simulation`
- 피드백 반영 백업: `backup_20260828_before_feature_guide_v2`
- 자동 도움말 반영 백업: `backup_20260828_before_feature_guide_v3`

### 2026-08-27 (부산 강희승 우수사례 영상 추가)
**교육자료 우수사례에 부산 강희승 영상을 신규 등록**
- 제목을 `[우수사례] 부산강희승`으로 등록하고 제공된 YouTube 영상 연결
- 기존 우수사례 목록 다음 순서로 노출하고 어드민 수정·숨김·정렬 기능 유지
- `files/`와 루트 배포용 파일을 동기화하고 서비스 워커를 `2026-08-27-v49`로 갱신
- 백업: `backup_20260827_before_busan_best_practice_v49`

### 2026-08-27 (9월 상담력 인사이트 업로드)
**2026년 9월 상담력 인사이트 PDF를 교육자료에 추가**
- 전달받은 5페이지 PDF를 전체 렌더링해 글자·표·이미지 깨짐이 없음을 확인
- PDF를 `insights/insight-2026-09.pdf`로 추가하고 교육자료 최신 항목에 연결
- 화면 표시 제목과 노출 여부 등은 기존 어드민에서 수정할 수 있도록 유지
- `files/`와 루트 배포용 파일을 동기화하고 서비스 워커를 `2026-08-27-v48`로 갱신
- 백업: `backup_20260827_before_september_insight_v48`

### 2026-08-25 (목표·성장 관리 기능 팀장 보고용 PPT 제작)
**상담 MASTER의 수수료 계산기와 성장 MAP 자가진단 기능 제안을 7장 보고자료로 구성**
- 상반기와 8월의 신청·진행·결제 평균 차이를 바탕으로 월·상품군 맥락 반영 필요성 정리
- 목표 수수료·상품군별 실적 입력 → 예상 수수료 → 성장 MAP → 보완 행동·교육 추천 흐름 제안
- 유아·초등·중등의 현장 특성을 구분하고 채널은 원인 단정 없이 자가점검 질문으로 표현
- 모바일 계산기와 성장 리포트 시뮬레이션 화면을 보고자료에 포함
- 학교 위치 기능은 제외하고 1개월 파일럿 검토 요청안으로 마무리
- 운영 서비스 파일과 Supabase 데이터는 변경하지 않음
- 피드백 반영본은 신규 목표관리 제안이 아닌 기존 활동일지·팀장교육·월 팀미팅을 연결하는 지원 기능 관점으로 6장 재구성
- 매니저 자가계산·수시 팔로업, 팀장의 공통 화면 기반 코칭, 조직의 목표관리 습관화를 기대효과로 정리
- 최종 시뮬레이션 중심 보고안은 텍스트를 축소하고 목표 입력·수수료 결과·성장 MAP·유형별 교육 추천 화면을 7장에 확대 배치
- 성과 우수와 전환 보완 예시 데이터를 각각 적용한 별도 모바일 리포트 이미지를 추가해 맞춤 안내 차이를 시각화

### 2026-08-13 (모바일 링크 접속 고정 프레임·RP 도움 영상)
**카카오톡 내부 브라우저와 아이폰·갤럭시의 서로 다른 표시 높이에서도 글자를 줄이지 않고 주요 버튼을 유지하도록 개선**
- 첫 접속 실제 브라우저 높이를 앱 프레임 기준으로 고정해 주소창 변화 시 전체 레이아웃 흔들림 완화
- 화면 전체 대신 각 화면의 내용 영역만 스크롤하는 앱 셸 구조로 정리
- 퀴즈의 남은 시간과 현재 직급을 한 줄 영역으로 압축하되 기존 글자 크기는 유지
- `정답 제출`·`다음 문제` 버튼을 퀴즈 화면 하단 고정 영역으로 이동해 긴 문제에서도 즉시 조작 가능
- 롤플레잉 룰렛 상단 오른쪽에 `도움영상` 버튼을 추가하고 팀별 RP 진행 안내 유튜브 영상 연결
- 모바일은 기존 앱 내 영상 화면, PC는 유튜브 새 탭으로 열리도록 기존 기기별 정책 유지
- RP 도움 영상의 복귀 버튼을 `롤플레잉`으로 구분해 영상 종료 후 로그인이나 교육자료 화면으로 잘못 이동하지 않도록 보완
- 기존 퀴즈 채점·포인트·룰렛 추첨·페이지 이동 로직은 변경하지 않음
- 서비스 워커 캐시 버전을 `2026-08-13-v42`로 갱신

### 2026-08-12 (교육자료 2개 자료실·우수사례 어드민 구축)
**시뮬 검수안대로 교육자료를 상담력 인사이트와 우수사례로 분리하고 운영 관리 기능 추가**
- 교육자료 상단 제목과 안내 문구를 `교육 자료` 중심으로 변경
- 전체·초등·중등·상담 필터를 제거하고 `상담력 인사이트`·`우수사례` 2개 탭 추가
- 기존 상담력 인사이트 14개와 연결 영상·PDF 열람 방식을 그대로 유지
- 제공된 유튜브 우수사례 12편을 제목·썸네일형 재생 카드로 추가
- 우수사례 화면에서 설명·분류·별도 영상 버튼 없이 제목과 재생 썸네일만 노출
- 어드민 교육자료에 우수사례 추가·제목·유튜브 링크·노출 여부·순서·삭제 관리 기능 추가
- 신규 설정 키 `best_practices_v1`을 사용해 기존 교육자료·퀴즈·포인트 데이터와 분리
- 서비스 워커 캐시 버전을 `2026-08-12-v41`로 갱신

### 2026-08-12 (교육자료·우수사례 분리 시뮬레이션)
**운영본을 변경하지 않고 교육자료 화면의 2개 자료실 구조를 별도 시뮬로 제작**
- 상단 명칭을 `교육 자료`로 변경하고 안내 문구를 상담력 인사이트·우수사례 중심으로 정리
- 기존 전체·초등·중등·상담 필터 대신 `상담력 인사이트`와 `우수사례` 2개 탭 구성
- 기존 월별 상담력 인사이트 14개를 PDF·연계 영상 구조로 유지
- 제공된 유튜브 12편의 실제 제목을 확인해 정착교육·인터뷰·하이라이트 배지와 영상 카드로 구성
- 운영 index·admin·service worker와 Supabase 데이터는 변경하지 않음
- 우수사례 카드에서 설명·구분 배지·별도 영상 버튼을 제거하고 제목과 썸네일 재생만 노출하도록 간소화
- 향후 어드민 관리 항목을 제목·유튜브 링크·노출 여부·정렬 순서로 최소화하는 구조 확정

### 2026-08-11 (배경음악 교체)
**짧게 반복되던 Web Audio 전자음을 2분 18초 길이의 밝은 교육용 배경음악으로 교체**
- Pixabay의 `Education - Modern Electronic Corporate Music`(SunnyVibesAudio) 공식 MP3를 프로젝트 자산으로 추가
- 첫 터치 이후 재생되는 아이폰 호환 흐름과 음소거 버튼을 유지
- 재생·음소거 전환 시 음량이 부드럽게 변하도록 페이드 처리 추가
- 음원을 반복 재생하되 기존 퀴즈·포인트·화면 이동 로직은 변경하지 않음
- 음원 출처와 Pixabay Content License 정보를 `assets/audio/LICENSE.txt`에 기록
- 서비스 워커 캐시 버전을 `2026-08-11-v40`으로 갱신

### 2026-08-11 (모바일 선택 영역·가독성·룰렛/랭킹 보완)
**50대 현장 사용자를 기준으로 잘림과 작은 글자, 룰렛 선택 텍스트 누락을 개선**
- 상담카드 학년 선택을 가로 스크롤 대신 3열 2행으로 재배치해 모든 학년을 한 화면에 노출
- 거절극복 유형을 2열 자동 줄바꿈으로 변경해 오른쪽 항목이 잘리지 않도록 수정
- 룰렛 가운데 강조 레이어와 텍스트의 쌓임 순서를 바로잡아 현재 선택 항목 누락 방지
- 룰렛·이달의 랭킹을 시스템 한글 폰트로 통일하고 주요 제목·항목·점수 글자를 확대
- 홈의 미션 및 메뉴 설명 문구를 정리하고 핵심 메뉴명을 확대해 정보 밀도를 낮춤
- 상단의 비공식 `ELIHi` 표식을 제거하고 `상담 MASTER`만 표시
- 서비스 워커 캐시 버전을 `2026-08-11-v39`로 갱신

### 2026-08-11 (PC PDF 열람 방식 분리)
- 모바일에서는 기존 화면 맞춤 PDF.js 뷰어 유지
- PC에서는 PDF 원본을 새 탭의 브라우저 기본 뷰어로 직접 연결
- PC 최초 PDF.js·대용량 파일 동시 로딩 지연을 줄이고 상담 MASTER 교육자료 화면은 유지
- PDF 파일·교육자료 데이터·모바일 복귀 로직은 변경하지 않음
- 서비스 워커 캐시 버전을 `2026-08-11-v38`로 갱신

### 2026-08-11 (PC 교육영상 재생 호환성 개선)
- 모바일에서는 기존 앱 내 YouTube 영상 뷰어 유지
- PC에서는 브라우저별 임베드 차단을 피하도록 YouTube 원본을 새 탭으로 직접 연결
- 새 탭 재생 방식으로 상담 MASTER 교육자료 화면은 그대로 유지
- 영상 주소·교육자료 데이터·모바일 복귀 로직은 변경하지 않음
- 서비스 워커 캐시 버전을 `2026-08-11-v37`로 갱신

### 2026-08-11 (교육자료 버튼 문구·정렬 보정)
- 교육자료의 `PDF 읽기` 문구를 `자료 읽기`로 변경
- `자료 읽기`와 `영상 보기` 버튼 글자를 가로·세로 중앙 정렬
- PDF·영상 연결 및 복귀 동작은 변경하지 않음
- 서비스 워커 캐시 버전을 `2026-08-11-v36`으로 갱신

### 2026-08-11 (직관적 아이콘·상담카드 위계 보강)
**전날 모바일 구조를 유지하면서 친근함과 기능 인지성을 복원**
- 프로젝트 이미지 자산을 전수 확인하고 식물 성장 캐릭터 5단계 경로와 파일 무결성을 확인
- `Q/C/R/RP/#/i/K` 문자 아이콘을 제거하고 메뉴 성격이 바로 보이는 기능 이모지로 교체
- 홈 주요 메뉴의 보라색 강조를 부드럽게 조정하고 핵심·보조 메뉴의 시각적 위계 유지
- 하단 내비게이션도 홈·성장·퀴즈·랭킹·마이를 직관적인 아이콘으로 변경
- 상담카드 상단 알파벳 표식을 과목별 아이콘으로 교체
- 질문·공감·추천 콘텐츠·추천 멘트를 오렌지·라벤더·민트·피치 계열로 구분하고 중첩 테두리를 축소
- 추천 콘텐츠를 화살표가 있는 터치형 카드로 변경하고 선택 시 콘텐츠명 복사 지원
- 상담카드와 거절극복의 추천 멘트를 인용 카드로 강조하고 `멘트 복사` 기능 연결
- 기존 식물 이미지, API, 데이터, 포인트 및 페이지 이동 로직은 변경하지 않음
- 서비스 워커 캐시 버전을 `2026-08-11-v35`로 갱신

### 2026-08-10 (모바일 UX 전면 리팩터링)
**레퍼런스의 정보 구조와 인터랙션을 실제 운영 화면에 반영**
- 공통 색상·간격·타이포·카드·버튼 토큰을 추가해 화면별로 겹치던 스타일을 하나의 모바일 디자인 시스템으로 통일
- 홈을 브랜드 헤더 → 성장 프로필 → 오늘의 미션 → 핵심 메뉴 → 빠른 메뉴 → 성장 배너 순서로 재구성
- 홈·성장여정·퀴즈·랭킹·마이로 이동하는 하단 내비게이션을 추가하고 퀴즈 집중 화면에서는 자동 숨김
- 성장여정을 식물 캐릭터 중심의 세로 타임라인과 현재·완료·잠금 상태로 정리
- 퀴즈 상단 정보를 압축하고 질문 20px, 보기 58px, 제출 버튼 56px로 모바일 가독성과 터치 동선 개선
- 상담카드 선택 단계를 STEP 구조로 정리하고 거절극복 응대 멘트에 복사 버튼 추가
- 교육자료를 월·제목·종류·PDF/영상 CTA가 명확한 압축 목록과 필터 구조로 개선
- 룰렛을 START → 스크롤 → STOP → 선택 결과 흐름으로 변경하고 랭킹에서 내 순위와 본인 행 위계 강화
- 360·375·390·430px에서 가로 넘침과 하단 메뉴 가림을 검수하고 주요 터치 영역을 44px 이상으로 보강
- 기존 Supabase 데이터, 포인트 계산, 퀴즈 기록, 어드민 설정 구조는 변경하지 않음
- 서비스 워커 캐시 버전을 `2026-08-10-v34`로 갱신

### 2026-08-10 (5단계 식물 성장 캐릭터 전면 적용)
**등급 시스템의 이모지 표현을 제거하고 포인트와 자동 연동되는 식물 캐릭터 5종으로 통일**
- 첨부 레퍼런스를 바탕으로 신입 새싹·열공 독서·여유 커피·베테랑 꽃나무·전설 황금열매 캐릭터를 각각 제작
- 단색 배경 제거 후 투명 PNG로 변환해 `assets/rank-plants/`에 저장
- 작업 전 운영 index/service worker를 `backup_20260810_before_plant_rank_characters`에 백업
- 홈 프로필 캐릭터를 연한 원형 배경의 큰 식물 이미지와 LEVEL 배지로 교체
- 포인트 변경 시 기존 5단계 경계값에 따라 캐릭터 이미지가 자동 변경되도록 연결
- 성장여정의 모든 단계 썸네일을 식물 이미지로 교체하고 완료·현재·잠금 상태의 시각적 위계 적용
- 퀴즈·결과·레벨업·랭킹 등급 표현에서도 시스템 이모지 대신 동일한 식물 자산 사용
- 모든 이미지에 `object-fit: contain`을 적용해 비율 유지 및 잘림 방지
- 어드민의 직급 이모지 입력 필드를 제거하고 기존 포인트·데이터·이동 로직은 유지
- 서비스 워커 캐시 버전을 `2026-08-10-v33`으로 갱신

### 2026-08-10 (인트로 앱 이름 강조)
**`엘리하이 상담 MASTER`가 장식 배지가 아닌 앱 이름으로 먼저 인식되도록 확대**
- 작업 전 운영 index/service worker를 `backup_20260810_before_intro_brand_title`에 백업
- 앱 이름의 글자 크기·굵기·여백을 확대하고 브랜드 헤더형 카드로 변경
- 374px 이하 화면에서도 제목과 겹치지 않도록 별도 크기 적용
- 서비스 워커 캐시 버전을 `2026-08-10-v32`로 갱신

### 2026-08-10 (프로필 카드·인트로 정렬·오답노트 노출 개선)
**직급과 성장 상태를 한눈에 읽는 프로필로 바꾸고 모바일 핵심 기능 노출을 정리**
- 작업 전 운영 index/admin/service worker를 `backup_20260810_before_profile_intro_review_admin`에 백업
- 어드민에서 더 이상 사용하지 않는 정답 응원 메시지 입력·저장 영역 삭제
- 인트로 부제목과 네 가지 기능 설명에 동일한 줄간격·줄바꿈·한글 단어 묶음 규칙 적용
- 프로필의 작은 사람 이모지를 단계별 색상의 큰 인물 실루엣과 `LEVEL` 배지로 교체
- 프로필 성장여정 버튼을 현재 직급·다음 직급·상세 단계 이동이 명확한 카드형 버튼으로 개선
- 더보기의 중복 성장가이드 버튼과 어드민 편집 필드를 제거
- 오답노트를 더보기 3번째 메뉴에 항상 표시하고 저장된 오답 수를 함께 노출
- 서비스 워커 캐시 버전을 `2026-08-10-v31`로 갱신

### 2026-08-10 (퀴즈 다음 문제 동선·프로필 성장여정·메뉴 어드민 확장)
**모바일 퀴즈 진행 동선을 짧게 만들고 신규 핵심 메뉴를 어드민 편집 항목에 추가**
- 작업 전 운영 index/admin/service worker를 `backup_20260810_before_quiz_flow_admin_growth`에 백업
- 정답·오답 결과를 화면 중앙의 짧은 자동 소멸 팝업으로 변경
- 하단 정답 설명·응원 박스를 숨기고 제출 버튼 위치에 `다음 문제` 또는 `결과 보기` 버튼이 바로 나타나도록 개선
- 채점·포인트 적립·오답 저장·타이머 로직은 그대로 유지
- 홈 프로필 안에 현재 직급에서 다음 직급으로 이어지는 작은 성장여정 바로가기 추가
- 어드민 대시보드 메뉴 카드에 상담 핵심카드·교육자료 제목과 설명 편집 필드 추가
- 서비스 워커 캐시 버전을 `2026-08-10-v30`으로 갱신

### 2026-08-10 (교육자료 직전 화면 복귀·어드민 편집 안내 보강)
**PDF·영상 열람 후 로그인 화면이 아닌 교육자료 목록으로 바로 돌아오도록 개선**
- 작업 전 운영 index/admin/PDF·영상 뷰어/service worker를 `backup_20260810_before_archive_navigation_admin`에 백업
- PDF·영상 이동 직전에 로그인 사용자와 교육자료 복귀 상태를 세션에 보관
- 아이폰 브라우저가 이전 페이지를 다시 불러오는 경우에도 로그인 정보를 복원해 교육자료 목록을 자동 표시
- PDF·영상 전용 화면의 `교육자료` 버튼을 항상 교육자료 목록 복귀 주소로 연결
- 어드민 상담카드·거절극복 및 교육자료에서 수정 가능한 항목과 저장 버튼 반영 방식을 명확히 안내
- 서비스 워커 캐시 버전을 `2026-08-10-v29`로 갱신

### 2026-08-10 (어드민 저장 완료 알림 오류 수정)
**서버 저장 후 잘못 표시되던 실패 팝업을 정상 저장 완료 안내로 수정**
- 작업 전 운영용 `admin.html`을 `backup_20260810_before_admin_toast_fix`에 백업
- 상담카드·교육자료 저장 함수의 잘못된 `showToast` 호출을 기존 공통 `toast` 함수로 교체
- 2026년 2월 교육자료 제목·영상 링크·페이지 수·정렬 순서가 Supabase에 실제 저장됐음을 확인
- 저장은 완료됐지만 알림 오류 때문에 실패로 오인되던 문제를 해소
- 서비스 워커 캐시 버전을 `2026-08-10-v28`로 갱신

### 2026-08-10 (로그인 이름·전화번호 기기 저장)
**재접속 시 이름과 휴대폰 번호가 자동 입력되도록 로그인 편의 기능 추가**
- 작업 전 운영 파일을 `backup_20260810_before_login_remember`에 백업
- 로그인 성공 시 선택된 경우 이름·휴대폰 번호를 현재 기기의 브라우저 저장소에 보관
- 다음 접속 시 저장된 정보를 자동 입력하되 자동 로그인은 하지 않도록 구성
- `이 기기에 로그인 정보 저장` 선택 항목과 `저장 정보 지우기` 기능 추가
- 공동 기기에서는 체크 해제 시 저장 정보를 남기지 않도록 처리
- 개인정보가 별도 서버나 설정 테이블에 추가 저장되지 않도록 제한
- 교육자료는 PDF·영상 링크가 동일해도 제목·페이지·노출 등 다른 수정값 저장 가능함을 확인
- 서비스 워커 캐시 버전을 `2026-08-10-v27`로 갱신

### 2026-08-10 (어드민 저장 검증·상태 표시 개선)
**상담카드와 교육자료가 저장 버튼을 눌렀을 때만 확정되고 서버 저장 여부가 명확히 보이도록 개선**
- 작업 전 어드민 운영 파일을 `backup_20260810_before_admin_save_fix`에 백업
- 기존 저장 API가 실패 응답을 확인하지 않던 문제 수정
- 저장 요청 성공 후 서버 값을 재조회해 입력 내용과 일치해야만 저장 완료 처리
- 입력 변경 시 `수정됨 · 저장 버튼을 눌러주세요` 상태 표시
- 저장 중 버튼 중복 클릭 방지와 `저장 중...` 표시 추가
- 성공 시 초록색 저장 완료 시각, 실패 시 빨간색 오류와 원인 표시
- 교육자료뿐 아니라 상담카드·거절극복 저장에도 동일한 검증 적용
- 현재 2025년 7월 교육자료가 서버에 실제 저장된 상태임을 확인
- 서비스 워커 캐시 버전을 `2026-08-10-v26`으로 갱신

### 2026-08-10 (퀴즈 제출 버튼·성장 아이콘·추천 콘텐츠 가독성 수정)
**퀴즈 화면의 시인성과 상담카드 3·4단계 정보 위계를 개선**
- 작업 전 운영 파일을 `backup_20260810_before_quiz_card_fix`에 백업
- 퀴즈 `정답 제출` 버튼 글자를 흰색으로 고정하고 비활성 상태에서도 대비 확보
- 퀴즈 상단에 크게 표시되던 직급 성장 이모지 영역을 제거하고 직급명·진행 막대만 유지
- 상담카드 1~4단계는 상담 흐름 유지를 위해 모두 보존
- 추천 콘텐츠를 작은 태그 대신 16px 한 줄 목록형 카드로 확대
- 질문·공감보다 추천 콘텐츠·추천 멘트가 강조되도록 시각적 위계 조정
- 서비스 워커 캐시 버전을 `2026-08-10-v25`로 갱신

### 2026-08-10 (상담카드·거절극복 모바일 가독성 개선)
**50대 상담 매니저가 휴대폰에서 빠르게 읽을 수 있도록 문장 배열과 글자 체계 개선**
- 작업 전 운영 파일을 `backup_20260810_before_card_readability`에 백업
- 상담카드 본문 글자를 16~17px, 줄 간격을 1.75~1.78로 확대
- 단계 번호와 제목을 한 줄 헤더로 묶고 본문이 카드 전체 폭을 사용하도록 구조 변경
- 질문은 물음표 기준, 긴 추천·응대 멘트는 쉼표와 줄바꿈 기준으로 의미 단위 자동 분리
- 추천 멘트를 별도 인용 상자로 강조하고 단계별 은은한 배경색 적용
- 추천 콘텐츠 태그, 학년·과목·거절유형 버튼의 글자와 터치 영역 확대
- 화면에 불필요한 상담카드·거절극복 관리번호 숨김
- 기존 카드 데이터와 어드민 저장 구조는 변경하지 않음
- 서비스 워커 캐시 버전을 `2026-08-10-v24`로 갱신

### 2026-08-07 (사람 아이콘 전환·성장가이드 간소화)
**성장 표현을 메뉴 이모지와 어울리는 작은 사람 아이콘으로 바꾸고 중복 영역 제거**
- 신입 `🙋‍♀️` → 열공 `👩‍🏫` → 여유 `😊` → 베테랑 `👩‍💼` → 상담왕 `👸`로 변경
- 메인 성장 아이콘을 64px로 축소해 기능 메뉴의 시각적 우선순위 강화
- 중복 노출되던 `복고양이 성장 도감` 제목과 5개 카드 영역 삭제
- 기존 성장 단계 목록의 높이, 여백, 아이콘, 상태표시를 축소해 답답함 개선
- 서비스 워커 캐시 버전을 `2026-08-07-v23`으로 갱신

### 2026-08-07 (캐릭터 중심 축소·성장 심볼 전환)
**큰 이미지 캐릭터를 제거하고 서비스 기능을 방해하지 않는 작은 성장 아이콘으로 단순화**
- 신입 `씨앗` → 열공 `새싹` → 여유 `화분` → 베테랑 `꽃` → 상담왕 `황금나무`로 전환
- 메인에서는 88px 이하의 작은 성장 배지로만 노출
- 성장가이드에서는 5단계를 한 줄 도감으로 표시해 직급 흐름을 직관적으로 확인
- 퀴즈·결과·성장 화면의 기존 캐릭터 자리도 동일한 성장 심볼로 통일
- 기존 직급·포인트 연동은 그대로 유지
- 서비스 워커 캐시 버전을 `2026-08-07-v22`로 갱신

### 2026-08-07 (성장 도감·현장상담 캐릭터·메인 압축)
**성장 캐릭터의 전체 노출과 모바일 첫 화면 정보 밀도를 개선**
- 성장가이드에 신입부터 전설의 상담왕까지 복고양이 5종 전체 도감 추가
- 현재·달성·목표 상태와 직급별 포인트 구간을 캐릭터 카드에 함께 표시
- 베테랑 캐릭터의 헤드셋을 제거하고 태블릿·현장 상담자료 중심으로 교체
- 메인 프로필 영역 높이, 캐릭터, 글자, 여백을 축소해 핵심 메뉴가 더 빨리 보이도록 조정
- 기존 직급·포인트·퀴즈 기능은 변경하지 않음
- 서비스 워커 캐시 버전을 `2026-08-07-v21`로 갱신

### 2026-08-07 (복고양이 부자 성장 캐릭터 교체)
**너구리 캐릭터를 제거하고 직급이 올라갈수록 풍요로워지는 복고양이 매니저로 교체**
- 신입: 동전 하나와 빈 지갑, 새싹
- 열공: 교재와 동전 저금통
- 여유: 커피, 가방, 모아진 동전
- 베테랑: 상담 헤드셋, 태블릿, 골드카드와 고급 가방
- 전설의 상담왕: 왕관, 빛나는 상담 교재, 황금 코인과 최종 복장
- 기존 직급·포인트 자동 연동 로직은 그대로 유지
- 서비스 워커 캐시 버전을 `2026-08-07-v20`으로 갱신

### 2026-08-07 (직급 연동 다마고치형 성장 캐릭터)
**누적 포인트 직급에 따라 같은 너구리 코치가 다섯 단계로 성장하도록 연동**
- 신입 상담사: 연필과 새싹을 든 초보 캐릭터
- 열공 상담사: 안경과 교재를 든 학습 캐릭터
- 여유 상담사: 커피와 태블릿을 든 안정형 캐릭터
- 베테랑 상담사: 헤드셋과 상담 보드를 든 전문가 캐릭터
- 전설의 상담왕: 왕관과 빛나는 교재를 든 최종 캐릭터
- 홈·퀴즈·결과·성장 화면의 캐릭터를 기존 직급 판정 함수와 자동 연동
- 서비스 워커 캐시 버전을 `2026-08-07-v19`로 갱신

### 2026-08-07 (전체 앱 디자인 시스템 통일 및 캐릭터 교체)
**접속·로그인부터 퀴즈와 자료 화면까지 전체 UI를 현대적인 교육 앱 스타일로 통일**
- Jua 중심 혼합 폰트를 모바일 시스템 한글 폰트 기반의 굵고 읽기 쉬운 타이포그래피로 교체
- 접속·로그인·홈·퀴즈·결과·룰렛·성장·상담카드·교육자료의 배경, 카드, 버튼, 입력창, 상단바 통일
- 사실적인 기존 3D 캐릭터를 단순하고 귀여운 2.5D 너구리 학습 코치로 교체
- 기존 기능·데이터·화면 전환 함수는 변경하지 않음
- 서비스 워커 캐시 버전을 `2026-08-07-v18`로 갱신

### 2026-08-07 (홈 UI 프리미엄 리디자인)
**기존 기능을 유지한 채 모바일 홈 화면을 현대적인 교육 앱 스타일로 개선**
- 점수·직급·성장률 영역을 한 장의 입체형 성장 대시보드로 재구성
- 상담 퀴즈·상담 핵심카드·교육자료에 부드러운 깊이감, 큰 글자, 명확한 색상 위계 적용
- 룰렛·랭킹·성장가이드는 작은 보조 메뉴로 유지하면서 터치 영역을 충분히 확보
- 상담마스터 전용 3D 너구리 학습 코치 캐릭터 자산 추가
- 기존 퀴즈·포인트·룰렛·랭킹·성장 기능 및 클릭 연결은 변경하지 않음
- 서비스 워커 캐시 버전을 `2026-08-07-v17`로 갱신

### 2026-08-07 (거절극복 추천 콘텐츠 구분)
**거절극복 콘텐츠도 상담카드와 같은 줄바꿈 관리 방식으로 통일**
- 거절극복 3단계 명칭을 `추천 콘텐츠`로 변경
- 줄바꿈·가운데점·쉼표로 구분된 콘텐츠를 화면에서 각각의 태그로 표시
- 어드민의 거절극복 추천 콘텐츠 입력란을 여러 줄 입력창으로 변경
- 저장 시 줄바꿈별 콘텐츠를 배열로 보관하고 기존 단일 문자열 데이터도 호환
- 서비스 워커 캐시 버전을 `2026-08-07-v16`으로 갱신

### 2026-08-07 (아이폰 PDF 폭 맞춤 뷰어)
**아이폰 Safari에서 PDF가 확대되어 좌우가 잘리는 문제 해결**
- 브라우저 기본 PDF iframe 대신 Mozilla PDF.js 기반 자체 뷰어 적용
- 문서 페이지를 기기 화면 폭에 자동 맞춤 렌더링
- 이전·다음 페이지 이동과 축소·확대 버튼 추가
- 상단 `교육자료` 복귀와 `원본` 열기 기능 유지
- 서비스 워커 캐시 버전을 `2026-08-07-v15`로 갱신

### 2026-08-07 (아이폰 가독성 보강)
**50대 상담 매니저 사용을 고려한 여백·글자 크기 개선**
- 홈 신규 메뉴에 좌우 여백을 추가하고 핵심·보조 메뉴 글자를 확대
- 상담카드·교육자료 화면을 아이폰 가장자리에서 8~12px 띄워 답답한 전체 폭 표시 개선
- 상담카드 질문·공감·추천 문구, 선택 버튼, 태그 글자를 약 15~25% 확대
- 교육자료 제목·연월·PDF 정보·열기 버튼 글자를 확대
- PDF·영상 전용 화면의 복귀 버튼과 제목 글자를 확대
- 서비스 워커 캐시 버전을 `2026-08-07-v14`로 갱신

### 2026-08-07 (PDF 복귀 화면 추가)
**상담력 인사이트 열람 후 교육자료로 돌아가는 전용 PDF 화면 구현**
- `pdf-viewer.html`에 상단 고정 `← 교육자료` 버튼과 PDF 내장 화면 추가
- 교육자료의 PDF 링크를 전용 화면으로 연결해 브라우저 빈 탭과 복귀 불편 개선
- PDF 원본 열기 보조 링크와 잘못된 파일 경로 차단 처리 추가
- 서비스 워커 캐시 버전을 `2026-08-07-v12`로 갱신

### 2026-08-07 (교육 영상 복귀 화면 추가)
**교육 영상도 상담력 인사이트와 동일한 복귀 흐름으로 통일**
- `video-viewer.html`에 상단 고정 `← 교육자료` 버튼과 YouTube 내장 재생 화면 추가
- 연계 영상 링크를 새 탭 대신 전용 영상 화면으로 연결
- YouTube 원본 열기 보조 링크와 영상 ID 검증 처리 추가
- 서비스 워커 캐시 버전을 `2026-08-07-v13`으로 갱신

### 2026-08-06 (상담카드·교육자료 로컬 통합)
**기존 운영 기능을 유지한 채 신규 콘텐츠 메뉴와 관리자 편집 기능 추가**
- 홈을 `자주 쓰는 메뉴`(상담퀴즈·상담 핵심카드·교육자료)와 `더보기`(룰렛·랭킹·성장가이드)로 재구성
- 기존 상담퀴즈·롤플레잉 룰렛·랭킹·성장·포인트 함수 연결은 그대로 유지
- 상담 핵심카드 29개와 거절극복 9개를 학년·과목별 모바일 화면으로 추가
- 2025년 7월~2026년 8월 상담력 인사이트 PDF 14개와 연계 영상 링크 통합
- 어드민에 `상담카드`, `교육자료` 탭 추가: 개별 항목·제목·PDF 주소·영상 링크·노출 여부 편집 지원
- 신규 설정 키 `counseling_cards_v1`, `education_archive_v1`만 사용해 기존 설정 및 점수 데이터와 분리
- PDF를 서비스 워커 캐시에 저장하지 않도록 처리하고 캐시 버전을 `2026-08-06-v10`으로 갱신
- 로컬 미리보기에서 홈, 상담카드, 거절극복, 교육자료, 어드민 편집 화면 클릭 동작 검수
- `files/`와 루트 배포용 파일을 동기화했으며 GitHub 배포는 아직 진행하지 않음

### 2026-08-06 (신규 화면 표시 오류 수정)
**실사용 미리보기에서 확인된 정렬·폰트·PDF 이동 문제 보완**
- 상담카드·교육자료 화면을 기존 모바일 `.wrap` 내부로 이동해 데스크톱에서도 가운데 정렬되도록 수정
- 홈 핵심 메뉴와 신규 콘텐츠 화면을 모바일 시스템 고딕 글꼴로 통일
- PDF 링크를 새 빈 탭이 아닌 현재 화면에서 직접 열도록 변경해 `브라우징 시작` 빈 화면 방지
- 로컬 HTTP 환경에서 2026년 8월 PDF 실제 주소 이동 확인
- 서비스 워커 캐시 버전을 `2026-08-06-v11`로 갱신

### 2026-08-06 (상담카드 시뮬레이터 재구성)
**엑셀 원본 기반 상담카드·거절극복 미리보기 구현**
- `상담카드_운영관리_분류수정_2026-07-29.xlsx`의 상담카드 30개를 학년·관심사별로 탐색 가능하게 반영
- 상담 핵심 흐름을 질문 어프로치 → 공감대 형성 → 추천 콘텐츠 → 추천 멘트로 구성
- 누락됐던 `거절반론` 9개 유형을 별도 `거절극복` 탭으로 추가
- 거절극복 카드를 먼저 공감 → 확인 질문 → 연결 콘텐츠 → 응대 멘트 → 마무리 연결 순서로 구성
- 상담 퀴즈·롤플레잉 룰렛·상담 핵심카드·교육자료가 한눈에 보이는 통합 홈과 각 화면의 홈 이동 버튼 복원
- 사용자 수정 엑셀의 추천 콘텐츠를 기준으로 완성한 상담 핵심카드 29개를 시뮬레이터에 교체 반영 (기존 퀴즈·룰렛·교육자료 비변경)
- 운영 서버에는 반영하지 않고 로컬 시뮬레이터에서만 검증

### 2026-07-28 (객관식 전환 준비)
**운영 중 엑셀 기반 객관식 문항 전환 및 포인트 정합성 개선**
- 기존 주관식 문항을 유지하면서 문항별 객관식 보기 4개와 공개 상태를 준비할 수 있도록 확장
- 어드민에 `.xlsx` 관리 양식 다운로드, 업로드 검증, 변경 미리보기 및 일괄 반영 추가
- 문제 ID 기준 업데이트로 엑셀 행 삭제가 실제 문항 삭제로 이어지지 않도록 보호
- 어드민의 미사용 문제별 200점 입력을 제거하고 실제 회차별 지급 포인트를 게임 설정으로 통합
- 첫 정답/2~3회차/4회차 이상 포인트 설정을 실제 게임 계산에 연동
- 기존에 저장만 되던 1회 출제 문제 수와 상품권 기준 설정을 실제 게임·어드민 집계에 연동
- Supabase 객관식 필드 추가 스크립트 `supabase_multiple_choice.sql` 작성
- 서비스 워커 캐시 버전을 `2026-07-28-v7`로 갱신

### 2026-07-28
**출제 균형·랭킹·접속 및 참여자 관리 개선**
- 문제 카테고리를 순환 배치해 특정 유형이 초반 문제에 편중되는 현상 개선
- 월간 랭킹 점수와 전체 누적 포인트를 분리하고, 월간 화면의 직급은 누적 포인트 기준으로 표시
- 로그인 시 일별 접속 이벤트를 저장하는 `access_logs` 연동 추가
- 어드민에 참여자 조회 탭 추가 (누적/월간 포인트, 최초/최근 접속일, 접속 일수)
- 참여자 목록 및 일별 접속 기록 CSV 다운로드 추가
- 기존 데이터 비변경 방식의 Supabase 생성 스크립트 `supabase_access_logs.sql` 추가
- 서비스 워커 캐시 버전을 `2026-07-28-v6`으로 갱신

### 2026-06-04
**초기 세팅 & 주요 기능 추가**
- `rand()` 함수 누락으로 인한 다음 문제 버튼 미노출 버그 수정
- GitHub Pages 배포 연결 (cje1520-collab/-)
- 대시보드 2×2 메뉴 그리드 추가 (퀴즈/룰렛/랭킹/성장)
- 롤플레잉 룰렛 기능 추가 (드럼 슬롯 + STOP)
- 랭킹 모달 추가 (나의 순위 + 월간 TOP5)
- 어드민 🎰 룰렛 탭 추가 (카테고리/항목 추가·수정·삭제)
- "대시보드" → "홈으로" 텍스트 전체 교체
- 매니저 호칭 기본값 변경 (선생님 → 매니저)

**버그 수정**
- 나가기 버튼 버그: `answered=true` 직후 nav 버튼 즉시 비활성화
- 랭킹 모달 `r.total` → `r.pts` 참조 오류 수정
- 다음 문제 버튼: 플로팅 고정 제거 → 문제 아래 중앙 인라인 표시
- `navAction()` 분리: 나가기는 항상 나가기, 다음 문제는 btn-next로만

**어드민 기능 강화**
- 대시보드 메뉴 카드 4개(퀴즈/룰렛/랭킹/성장) 제목·설명 어드민에서 편집 가능
- 어드민 ✏️ 텍스트 탭 → 🗂️ 대시보드 메뉴 카드 섹션 추가
- GT 기본값에 `menu_*_label/desc` 추가, `applyTexts()` 연동

**협업 문서**
- `AGENTS.md` 최초 생성 (프로젝트 구조, 기술 스택, 어드민 사용법, 작업 이력)
- 작업 전 보고 → 컨펌 → 진행 워크플로우 확립
- 작업 전 `backup_YYYYMMDD/` 백업 생성 규칙 확립

---

## 🔜 향후 예정 작업 (미확정)

- [ ] 롤플레잉 룰렛 주제별 스크립트 힌트 표시
- [ ] 추가 카테고리 (가격안내, 체험상담 등)
- [ ] 오답노트 강화 (날짜별 분류)


---

### 2026-08-06 (상담력 인사이트 아카이브 시뮬레이션)
**월별 PDF 교육자료 목록 및 제목 분리 관리 구조 추가**
- 2025년 7월부터 2026년 8월까지 상담력 인사이트 PDF 14개를 시뮬레이터 교육자료 화면에 최신순으로 구성
- 모바일 목록에 연월, 주제 제목, PDF 페이지 수를 표시하고 각 원본 PDF 열기 연결
- PDF 파일 경로와 화면 표시 제목을 `insight-archive.json` 데이터로 분리해 향후 어드민 제목 수정 구조 준비
- 기존 상담 퀴즈, 롤플레잉 룰렛, 상담 핵심카드 화면은 변경하지 않음
- 390px 모바일 화면 및 최신 PDF 연결 동작 확인


---

### 2026-08-06 (상담력 인사이트-교육영상 연계 시뮬레이션)
**월별 자료 카드 안에 PDF와 연계 영상 통합**
- 2026년 8월 인사이트에 신규 YouTube 교육 영상 연결
- 기존 교육 영상은 2026년 3월 인사이트에 연결하고 별도 교육 영상 카드는 제거
- 영상 보유 월에는 같은 카드에서 `PDF 읽기`와 `영상 보기`를 선택하도록 모바일 UI 구성
- 운영 파일과 비교해 시뮬레이터의 퀴즈·룰렛 내부 화면은 간이 목업이며 실제 운영 화면은 변경되지 않았음을 확인
- 390px 모바일 화면에서 14개 자료, 영상 2개, 가로 넘침 없음 확인


---

### 2026-08-06 (상담카드 모바일 디자인 개선 시뮬레이션)
**한글 가독성 및 연령별 카드 구분 강화**
- 전체 시뮬레이터 글꼴을 모바일 한글 가독성이 높은 시스템 고딕 계열로 변경
- 상담카드를 유아·초등 저학년·초등 중학년·초등 고학년·중등의 5개 색상 테마로 구분
- 연령별 병아리·토끼·여우·너구리·호랑이 캐릭터 표식 추가
- 선택 학년 버튼, 카드 테두리, 단계 번호, 콘텐츠 태그에 동일한 연령 색상 적용
- 390px 모바일 화면에서 유아 및 중등 카드 가독성, 버튼 간격, 가로 넘침 없음 확인


---

### 2026-08-06 (실서버 반영 전 안전 준비)
**운영본 백업·시뮬 보관·반영 위험 검토**
- 운영 루트와 `files`의 index/admin/service worker/.nojekyll을 `backup_20260806_before_cards_education`에 보관
- 백업 8개 운영 파일의 SHA-256이 원본과 모두 일치함을 확인하고 복구 안내서 작성
- 상담카드·교육자료 최종 시뮬, 데이터, 원본 엑셀, PDF 14개를 `simulation_20260806_cards_education`에 별도 보관
- 기존 퀴즈·포인트·랭킹·성장·룰렛 비변경 원칙과 회귀 테스트 목록을 `IMPLEMENTATION_REVIEW_20260806.md`에 작성
- PDF 약 24MB의 지연 로딩·캐시 제외 필요성과 향후 어드민 PDF 업로드 방식 결정을 확인
- 운영 파일 수정과 GitHub 배포는 아직 진행하지 않음


---

### 2026-08-06 (모바일 홈 메뉴 위계 시뮬레이션)
**핵심 3개·보조 3개 기능으로 홈 UX 재구성**
- 상담퀴즈를 전체 폭 핵심 카드로, 상담카드와 교육자료를 큰 2열 카드로 구성
- 룰렛·랭킹·성장가이드는 하단의 작은 빠른 메뉴 3개로 구성
- 320px에서는 핵심 카드 세로 배치, 360px 이상에서는 1+2열 배치하도록 반응형 적용
- 320·360·390·430px 화면에서 가로 넘침, 버튼 겹침, 주요 텍스트 영역 확인
- 최종 시뮬 버전을 14로 갱신하고 프로젝트 내 시뮬 보관본과 반영 검토 문서 동기화
- 운영 파일 수정과 GitHub 배포는 아직 진행하지 않음

### 2026-08-13 (상담카드 압축·상담 브로슈어 추가)
- 상담카드·거절극복 선택 영역을 현재 선택 요약과 `변경` 방식으로 접어 카드 내용을 먼저 표시
- 질문→공감→추천 콘텐츠→추천 멘트 흐름을 유지하고 복사 기능 제거
- 상담자료 탭과 모바일용 초등 상담 브로슈어 16페이지 추가, 중등은 준비 중 표시
- 브로슈어 열람 후 상담자료 탭 복귀 지원
- 백업: `backup_20260813_before_compact_cards_brochure_v43`
- 서비스 워커: `2026-08-13-v43`

### 2026-08-13 (중등 상담 브로슈어 추가)
- 중등 인쇄용 표지·내지를 모바일 열람 순서의 12페이지 단일 PDF로 재구성
- 약 46MB 원본을 약 2.8MB로 최적화하고 표지·본문·뒤표지 렌더링 검수
- 상담 핵심카드의 상담자료 탭에서 중등 브로슈어를 바로 열도록 연결
- 백업: `backup_20260813_before_middle_brochure_v44`
- 서비스 워커: `2026-08-13-v44`

### 2026-08-13 (학년 연계 브로슈어·선택 UI 강조)
- 상단 `상담자료` 탭을 제거하고 브로슈어를 상담카드의 선택 학년에 직접 연결
- 4~6세·7세는 브로슈어 미노출, 초등 학년군은 초등 자료, 중등은 중등 자료 표시
- 학년·과목 선택 영역을 보라색 강조 카드와 `다시 선택` 버튼으로 개선
- PDF 열람 후 기존에 보던 학년·과목 상담카드로 복귀하도록 선택 상태 저장
- 백업: `backup_20260813_before_inline_brochure_v45`
- 서비스 워커: `2026-08-13-v45`

### 2026-08-13 (인트로 호칭 깜빡임 제거)
- 초기 HTML의 `선생님` 임시 문구를 제거하고 첫 화면부터 `매니저님`으로 표시
- 관리자 설정 로딩 후에도 인트로는 설정된 호칭에 `님`을 한 번만 붙여 동일하게 유지
- 로그인·포인트·관리자 데이터 구조는 변경하지 않음
- 백업: `backup_20260813_before_intro_title_flicker_v46`
- 서비스 워커: `2026-08-13-v46`

### 2026-08-13 (브로슈어 교육 자료 상단 이동)
- 상담카드 안의 학년별 브로슈어 버튼 제거
- 교육 자료 안내 바로 아래에 초등·중등 브로슈어 2칸 고정 카드 배치
- 브로슈어 다음에 상담력 인사이트·우수사례 탭이 이어지는 정보 구조로 변경
- PDF 종료 시 교육 자료 화면으로 복귀
- 백업: `backup_20260813_before_education_brochures_v47`
- 서비스 워커: `2026-08-13-v47`
